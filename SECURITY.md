# Security policy

Do not report real API keys in GitHub issues. The router is intended for local use on `127.0.0.1` by default. Do not expose it publicly without authentication, TLS, and network controls.
